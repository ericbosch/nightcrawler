services:
  searxng:
    image: searxng/searxng:latest
    container_name: nightcrawler-searxng
    ports:
      - "127.0.0.1:8888:8080"
    volumes:
      - ./searxng:/etc/searxng
    restart: unless-stopped
